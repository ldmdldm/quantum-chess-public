# Blockchain Configuration
# -----------------------
# The URL of the RPC endpoint for the Core testnet
CORE_BLOCKCHAIN_URL=https://rpc.test2.btcs.network

# Your private key (KEEP THIS SECRET!)
# This is used to sign transactions when deploying contracts
CORE_PRIVATE_KEY=your_private_key_here

# Chain ID for the Core testnet (1114 for test2.btcs.network)
CORE_CHAIN_ID=1114

# Contract address (will be filled automatically after deployment)
CORE_CONTRACT_ADDRESS=deployed_contract_address_here

# Gas Configuration
# ----------------
# Gas price in gwei (optional, uses network default if not specified)
GAS_PRICE=5

# Gas limit for contract deployment (optional, estimated automatically if not specified)
GAS_LIMIT=3000000

# Database Configuration (if needed)
# ---------------------------------
DATABASE_URL=postgres://user:password@localhost/quantum_chess

# Deployment Settings
# ------------------
# Safety buffer for gas estimation (as a percentage, e.g., 20 = 20%)
GAS_BUFFER=20

